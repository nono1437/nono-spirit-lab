# 《星梦牌旅》ChatGPT Work 接手说明书

> 当前定位：V11 稳定母档的 Web 开发后代；现阶段优先视觉、打击感、真逐帧动作与训练场，不推进剧情。
> 建议专用域名：`dream.nono.love`
> 当前实验分支：`nono1437/nono-spirit-lab` → `stardream-frame-lab`

## 1. 版本与血统

- **V11**：最后一个完整稳定母档 / 唯一正式主干来源。
- **Web Baseline / Formal Preview 1.x**：基于 V11 的现行开发后代。
- **V5 / Arena**：只可作为角色与机制素材库，不得整体回灌覆盖 V11。
- 当前真机开发节点：**Formal Preview 1.3.2 · Real Frame Scaffold**。
- 当前主要实验角色：烬羽 → 炎羽凰姬。

任何冲突以：
1. nono 最新明确决定；
2. 最新可运行的 V11 后代包；
3. 本说明书；
为优先顺序。

## 2. 当前目标

只做下面这些，不扩写剧情、不重构整个项目：

- 把炎羽凰姬从“单张 PNG 位移”升级为**真实逐帧动作**。
- 每次连击切换到一张真实动作帧。
- 上一帧 / 上上帧以接近 1:1 模型大小保留为残影。
- 本体必须到达上一段动作的终点，并从该位置继续下一段，不要每击复位。
- 翅膀、武器、身体姿态要依靠真实帧变化，而不是仅靠 CSS 旋转缩放。
- 斩击使用现有 PNG atlas；旧的线形测试特效对烬羽应退役。
- 特效强度使用固定档位，避免忽轻忽重。
- 保留训练模式，用于无限时间 / 无限灵力 / 全牌 / 连击测试。

## 3. 炎羽凰姬真实动作帧

当前帧命名（Flat Root）：

- `ember-frame-00-hover.png`：悬浮 / 起始
- `ember-frame-01-charge.png`：蓄力
- `ember-frame-02-dash.png`：突袭
- `ember-frame-03-slash.png`：斩击
- `ember-frame-04-dance.png`：烈焰狂舞
- `ember-frame-05-judgment.png`：空中裁决
- `ember-frame-06-wingburst.png`：烈焰羽翼展开
- `ember-frame-07-finisher.png`：终焉 / 收束

建议第一轮动作顺序：

`00 → 01 → 02 → 03 → 04 → 05 → 06 → 02 → 03 → 04 → ...`

终止连击时切到 `07`，停留短时间后再回 idle。

## 4. 手感参数（先从这里调）

当前建议起点：

- 点击有效间隔：**145–180ms**
- 单帧动作可见时间：**180–240ms**
- 残影寿命：**320–420ms**
- 普通连击残影：2 帧
- 重击节点残影：3 帧
- 命中停顿（hit-stop）：30–55ms
- 每 5 HIT：重反馈
- 每 10 HIT：大斩 / 大爆点 / 更明显镜头冲击

原则：宁可动作稍慢，也不要“不到眨眼就播完”。

## 5. 特效规则

现有图集：

- `vfx-ember-slash-atlas.png`
- `vfx-ember-impact-atlas.png`
- `vfx-ember-magic-atlas.png`

渲染原则：

- 点击位置只作为**输入**，不要在手指下生成主要特效。
- 斩击 / 命中爆点应锁定敌人区域。
- 角色动作残影只在角色真实运动轨迹附近出现。
- PNG atlas 使用稳定尺寸与透明度档位，不随机出现超大核爆。
- 普通攻击尽量让敌人本体保持可见。
- 大型 magic atlas 只在高连击节点或终结使用。

## 6. WebCat / 发布硬约束

发布 ZIP 必须保持 **Flat Root**：

- 所有运行文件直接在 ZIP 根目录。
- 不允许嵌套运行目录。
- 页面跳转必须写明确文件名，例如 `./game.html`。
- 不要依赖目录自动寻找 `index.html`。
- 不要为了整理代码破坏 WebCat 兼容。
- 不要在 WebCat 发布包里随便加 Service Worker。

GitHub 源码仓可以有目录，但生成给 WebCat 的 ZIP 必须扁平化。

## 7. 回归测试

每次修改至少检查：

- `node --check game.js`
- ZIP 完整性
- ZIP 根目录无子目录
- `index.html` / `game.html` 本地资源引用全部存在
- 首页进入游戏
- 角色选择
- 训练模式
- 烬羽两次燃命必进入炎羽凰姬
- 烬羽没有梦游者专属牌串入
- 纸灵首击限伤不会误播斩杀
- 连击开始 / 连击结束
- 逐帧顺序正确
- 残影大小接近角色本体
- 本体到达动作终点，不每击复位
- 长时间训练连击不明显掉帧 / 不无限堆积 Canvas 对象

## 8. GitHub / 域名建议

建议正式为《星梦牌旅》准备独立站点：

`dream.nono.love`

阿里云 DNS：

- 记录类型：`CNAME`
- 主机记录：`dream`
- 记录值：`nono1437.github.io`
- TTL：10 分钟即可

但 **GitHub Pages 的 CNAME 必须绑定到承载《星梦牌旅》的独立 Pages 仓库**。
不要把现有 `play.nono.love`（nono Spirit Lab）直接抢占。

推荐新仓库名：

`nono1437/stardream-journey`

仓库创建后：
- Pages 开启；
- 仓库根目录 `CNAME` 写 `dream.nono.love`；
- 再把 Formal Preview / Work 开发版本迁入。

## 9. ChatGPT Work 工作方式

Work 可以持续把仓库当成一个项目来维护，适合：

- 接真实动作帧；
- 调整 `game.js` / `style.css`；
- 运行本地静态测试；
- 看 GitHub Actions；
- 维护变更记录；
- 一轮轮做视觉回归。

但真机 WebCat 的“手感”不能只靠自动测试判定。
最终仍要 nono 在手机上确认：

- 点击是否跟手；
- 残影是否太浓；
- 动作是否太快；
- 特效是否遮挡；
- 角色有没有“自己在打架”的感觉。

## 10. 禁止事项

- 不要把剧情素材自动实装。
- 不要把 V5 当成新主干。
- 不要删除 V11 / 稳定母档。
- 不要把六角色一次性全改成未验证的新动画系统。
- 不要在一次提交里同时重构卡组、存档、关卡与 VFX。
- 不要把烬羽逐帧实验失败等同于玩法失败；先回退表现层。
- 不要恢复“每击角色立刻回原位”的残影逻辑。

---

## 给 Work 的第一条任务

请以《星梦牌旅》Formal Preview 1.3.2 为当前实验基线，先只处理炎羽凰姬。

目标：
1. 将 8 张 `ember-frame-*` 真实动作帧接入连击；
2. 让每次有效点击推进一段真实动作；
3. 前 2–3 帧以 1:1 角色尺寸生成残影；
4. 本体停在动作终点，并从当前动作继续下一击；
5. 旧线形测试特效对烬羽退役；
6. slash / impact / magic atlas 强度固定分级；
7. 保留训练模式；
8. 每次改动后运行静态回归测试并报告结果；
9. 不改剧情、关卡数值、牌库平衡和其他角色。
